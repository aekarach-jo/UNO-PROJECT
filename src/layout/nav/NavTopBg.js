/* eslint-disable prettier/prettier */
import React from 'react';

const NavTopBg = () => {
  return (
    <div
      className="logo position-absolute d-none d-sm-block"
      style={{
        width: '404px',
        height: '404px',
        top: '-223px',
        left: '-62px',
        borderRadius: '50%',
        background: 'linear-gradient(to right bottom, rgba(255, 255, 255, 0.5), rgba(143, 206, 161, 0.5))',
      }}
    >
      ""
    </div>
  );
};
export default React.memo(NavTopBg);
